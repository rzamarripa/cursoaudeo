import 'package:flutter/material.dart';

class SelenePage extends StatelessWidget {
  const SelenePage
({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hola, soy Selene"),
    );
  }
}