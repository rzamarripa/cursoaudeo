import 'package:flutter/material.dart';

class CarlosPage extends StatelessWidget {
  const CarlosPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hola, soy Carlos"),
    );
  }
}