
import 'package:flutter/material.dart';

class MedranoPage extends StatelessWidget {
  const MedranoPage({Key key}) : super (key: key);

  @override
  Widget build( BuildContext context) {
    return Container( child: Text("Hola, soy Medrano"),
    );
  }
}