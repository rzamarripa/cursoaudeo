import 'package:flutter/material.dart';

class ZamarripaPage extends StatelessWidget {
  const ZamarripaPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Zamarripa"),
      ),
      body: Column(
        children: <Widget>[

        ],
      ),
    );
  }
}