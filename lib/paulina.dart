import 'package:flutter/material.dart';

class PaulinaPage extends StatelessWidget {
  const PaulinaPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Paulina"),
      ),
      body: Column(
        children: <Widget>[

        ],
      ),
    );
  }
}